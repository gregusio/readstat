# ============================================
# Readstat - Environment Variables
# ============================================
# Copy this file to .env and fill in the values:
#   cp .env.example .env
#
# NEVER commit .env to version control!
# ============================================

# --- Database ---
# Password for the SQL Server SA (admin) account
# Must meet SQL Server complexity requirements:
#   at least 8 characters, uppercase, lowercase, digit, special char
SA_PASSWORD=YourStrong!Passw0rd

# --- Backend ---
# Port the backend API listens on
BACKEND_PORT=5027

# ASP.NET environment (Development | Staging | Production)
ASPNETCORE_ENVIRONMENT=Development

# --- JWT Authentication ---
# Secret key used to sign JWT tokens (min 32 characters, keep this secret!)
# Generate one with: openssl rand -base64 48
JWT_KEY=your-super-secret-jwt-key-change-me-in-production
JWT_ISSUER=readstat
JWT_AUDIENCE=readstat

# --- CORS ---
# Comma-separated list of allowed frontend origins
# In production, set this to your actual deployed frontend URL
ALLOWED_ORIGINS=http://localhost:3000

# --- Frontend ---
# Port the frontend is served on
FRONTEND_PORT=3000

# Base URL of the backend API (as seen from the browser)
# In production, set this to your deployed backend URL
VITE_API_BASE_URL=http://localhost:5027/api
